<h2>Welcome, {{adminName}}</h2>
<p>Today is {{date}}</p>

<div class="row mt-4">
    <div class="col-md-4">
        <div class="card text-white bg-primary mb-3">
            <div class="card-body">
                <h5 class="card-title">Total Quizzes</h5>
                <p class="card-text fs-2">{{quizCount}}</p>
            </div>
        </div>
    </div>
</div>